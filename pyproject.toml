[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm_engineering"
version = "0.0.0"
description = "LLM engineering workspace"
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "owner"}]
dependencies = []
